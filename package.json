{
  "name": "@heat/lambda",
  "main": "index",
  "version": "0.3.2",
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/heat-js/lambda.git"
  },
  "bugs": {
    "url": "https://github.com/heat-js/lambda/issues"
  },
  "scripts": {
    "test": "./node_modules/.bin/jest --runInBand",
    "patch": "yarn publish --patch",
    "minor": "yarn publish --minor",
    "major": "yarn publish --major",
    "prepublish": "bash ./build.sh",
    "postpublish": "yarn cleanup",
    "cleanup": "rm -f ./{/**,}/*.js; rm -rf error; rm -rf middleware;"
  },
  "dependencies": {
    "@bugsnag/js": "^5.0.1",
    "@heat/container": "^0.0.1",
    "extendable-error": "^0.1.5"
  },
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-es2015-modules-commonjs-optimized": "^6.16.0",
    "coffeescript": "^2.3.1"
  },
  "babel": {
    "plugins": [
      "transform-es2015-modules-commonjs-optimized"
    ]
  }
}
